{
  "name": "reletix-survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:export": "wrangler d1 export reletix-survey --remote --output=responses.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
